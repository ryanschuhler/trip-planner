<%= form_for([@trip, @trip.members.build]) do |f| %>
  <span>
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </span>
  <span>
    <%= f.label :status %><br>
    <%= f.number_field :status %>
  </span>
  <span>
    <%= f.submit %>
  </span>
<% end %>
